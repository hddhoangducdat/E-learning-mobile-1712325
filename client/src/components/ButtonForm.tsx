import React from "react";
import { Button, StyleSheet } from "react-native";

export default function ButtonForm({ title, callBack }: any) {
  return <Button onPress={callBack} title={title} color={} />;
}

const styles = StyleSheet.create({});
