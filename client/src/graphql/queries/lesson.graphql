query Lesson($lessonId: Int!) {
  lesson(lessonId: $lessonId) {
    ...LessonFragment
    section {
      id
      name
    }
    track {
      ...TrackingLessonFragment
    }
    resource {
      id
      name
      url
      type
    }
  }
}
